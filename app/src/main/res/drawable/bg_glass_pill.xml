<?xml version="1.0" encoding="utf-8"?>
<!-- "Glass card" pill for the floating control bar shown during an active
     workflow: translucent so the real screen underneath stays visible,
     per spec ("তার নিচের স্ক্রিন ও বোঝা যায়"). True blur isn't available
     pre-API 31 without extra libraries, so this uses high translucency +
     a soft border as the glass approximation. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#CCFFFFFF" />
    <stroke android:width="1dp" android:color="#40FFFFFF" />
    <corners android:radius="28dp" />
</shape>
