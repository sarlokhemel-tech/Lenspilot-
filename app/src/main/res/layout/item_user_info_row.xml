<?xml version="1.0" encoding="utf-8"?>
<!-- One row of the "আমার তথ্য" table — see UserInfoAdapter. Two columns,
     the label column narrower than the value column (layout_weight 1
     vs 2, per the user's spec: "শিরোনাম এর কলাম একটু ছোট হবে"). Both
     EditTexts are multi-line/wrap-content so a longer value just grows
     the row taller instead of clipping — "যতো লিখবে ততো আসবে". -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="top"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <EditText
        android:id="@+id/userInfoRowLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginEnd="6dp"
        android:background="@drawable/bg_user_info_cell"
        android:padding="8dp"
        android:hint="@string/user_info_label_hint"
        android:textColor="@color/text_primary_light"
        android:textColorHint="@color/text_muted_light"
        android:textSize="13sp"
        android:minLines="1"
        android:inputType="textCapSentences|textMultiLine" />

    <EditText
        android:id="@+id/userInfoRowValue"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:layout_marginEnd="6dp"
        android:background="@drawable/bg_user_info_cell"
        android:padding="8dp"
        android:hint="@string/user_info_value_hint"
        android:textColor="@color/text_primary_light"
        android:textColorHint="@color/text_muted_light"
        android:textSize="13sp"
        android:minLines="1"
        android:inputType="textCapSentences|textMultiLine" />

    <ImageButton
        android:id="@+id/userInfoRowDelete"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center_vertical"
        android:background="@drawable/bg_circle_icon"
        android:padding="6dp"
        android:src="@drawable/ic_close"
        android:contentDescription="@string/user_info_delete_row" />

</LinearLayout>
